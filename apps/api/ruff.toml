line-length = 110
target-version = "py310"

[lint]
select = ["E", "F", "I", "B", "UP"]
# E501: line length — handled by formatter
# B008: function calls in argument defaults — required by FastAPI Depends/File/Query patterns
ignore = ["E501", "B008"]

[lint.per-file-ignores]
"tests/*" = ["E", "F"]
